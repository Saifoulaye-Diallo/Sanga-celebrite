{% extends "base.html.twig" %}

{% block body %}
<h1 class="tittle p-1">{{ article.title }}</h1>
    <div class="img-fluid">
        <img width="100%" src="/assets/blog/images/{{ article.image }}" alt="{{ article.title }}"/>
    </div>
    <div class="content">
        <p class="date">Ecris le {{ article.creatAt | date("m/d/Y") }} à {{ article.creatAt | date("H:m") }}</p>
        <h4 class="tittle">{{ article.title }}</h4>
        <p class="content text-justify">
            {{ article.content | raw }}
        </p>
    </div>
{% endblock %}